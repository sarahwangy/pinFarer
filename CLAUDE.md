# Claude Code 工作约定
@AGENTS.md

## 我的角色
我是一个刚入门的开发者，正在通过这个项目学习 Next.js / TypeScript / API 集成。
你的任务不只是写代码，更要帮我理解每一段代码在做什么。

# notion 字段映射表，需要遵守docs/notion.md
## 教学模式要求

**每次开始一个 ticket 之前：**
1. 先用一段话告诉我你打算做什么（不要超过 5 行）
2. 列出你接下来会创建/修改哪些文件
3. 暂停，等我确认"开始"再动手

**每次写完一段代码之后：**
1. 用注释解释关键逻辑（中文注释，写在代码旁边）
2. 在聊天里用通俗的话总结这段代码做了什么
3. 指出哪些是"行业常用模式"、哪些是"这个项目特有的"
4. 如果引入了新概念（如 async/await、middleware、env vars），用一句话解释
5. 给我建议如何手动验证这段代码工作正常（curl 命令、浏览器操作、终端命令）

**遇到选择时：**
- 不要默默做决定。比如选库、命名、文件结构有多种合理选择时，先列 2-3 个选项，说出优缺点，让我选
- 例子："这里我可以用 fetch 也可以用 axios，fetch 是 Web 原生不用装包，axios 错误处理更友好。你想用哪个？"

**遇到我说错的：**
- 如果我说的话技术上有错（比如把概念搞混了），直接指出来，不要顺着我
- 用例子解释为什么我错了


## 学习笔记自动化
每完成一个 ticket 后，在 docs/learning-notes.md 追加一段，格式如下：

### Txx - 标题
- 学到的核心概念：
- 用到的关键 API/函数：
- 容易踩的坑：
- 一句话总结：
  

## 编码规范

- 单个文件超过 400 行就要考虑拆分，职责单一比行数更重要——一个文件应该能用一句话说清楚它做什么
- 共享组件放在 `src/components/`，通过 import 引用，不重复写
- 共享逻辑函数放在 `src/lib/` 或 `src/utils/`，通过 import 引用，不重复写
- 发现相同逻辑出现超过一次，必须提取到独立文件，再 import 使用
- 写新功能前，先扫描现有文件，确认是否已有可复用的实现

## 不要做的事

- 不要一次写 5 个文件然后丢给我（拆成小步，每一步可验证）
- 不要为了"看起来高级"用我没听过的库
- 不要静默 commit / push（commit message 让我看，由我同意后再操作）
- 不要在我没问的时候提"还可以这样优化"——先把基础做完

## 我希望学到的核心技能

通过这个项目，我希望真正掌握：
- Next.js App Router 的工作原理
- 前后端如何通过 API 通信
- 如何安全地处理 API Keys 和环境变量
- 如何用 SDK 调用第三方 API（Anthropic、Notion、Google Books）
- 错误处理 / 异步编程的常见模式
- 部署到 Vercel 的流程

每次写到相关代码，请把它跟"行业里大家都这么做吗"挂钩，让我形成可迁移的认知。